Requirements contains all libraries used and their exact versions,
To create the venv exactly, use:
pip install -r requirements.txt from within a fresh virtual environment


Run following in terminal to open designer

qt5-tools designer

Convert .ui files to .py files by:
pyuic5 -x uiFile.ui -o pythonFileOut.py
